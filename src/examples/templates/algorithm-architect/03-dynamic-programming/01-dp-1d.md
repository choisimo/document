# 1차원 DP (1D Dynamic Programming)

## 개요

| 항목 | 설명 |
|------|------|
| **Use Case** | 피보나치, 계단 오르기, 최대 부분 합 |
| **Components** | DP Table (1차원 배열) |
| **Constraint** | 이전 상태로부터 현재 상태 도출 |
| **시간 복잡도** | state 수 × state당 transition 수; 예제는 O(N) |

---

## 적용 범위와 검증 기준

- **범위:** 1D state index를 사용하는 DP pattern이며 모든 문제의 시간이 `O(N)`인 것은 아닙니다. state 수 × state당 transition 수로 time을 계산합니다.
- **전제:** state 의미, base case, dependency direction, iteration order와 impossible-state sentinel을 먼저 정의합니다. rolling memory는 더 오래된 state가 필요 없을 때만 가능합니다.
- **실패 조건:** initialization, negative/empty input, overflow, unreachable state와 update order로 인한 값 재사용을 포함합니다.
- **완료 증거:** 작은 입력의 brute force/reference와 각 state recurrence를 비교하고 boundary와 memory optimization 전후 결과가 같은지 확인합니다.

---

## 기본 템플릿

```python
# [1D DP 템플릿: 아키텍트 버전]
# Use Case: 피보나치, 계단 오르기, 최대 부분 합
# Components: DP Table (1차원 배열)
# Constraint: 이전 상태로부터 현재 상태 도출

def dp_1d_template(n):
    # 1. 초기화 (Initialization Layer)
    #    - DP 테이블 생성 및 베이스 케이스 설정
    dp = [0] * (n + 1)
    dp[0] = 1  # 베이스 케이스
    dp[1] = 1  # 베이스 케이스
    
    # 2. 상향식 계산 (Bottom-Up Calculation)
    for i in range(2, n + 1):
        # 3. 점화식 (Recurrence Relation)
        #    - 이전 상태들로부터 현재 상태 계산
        dp[i] = dp[i-1] + dp[i-2]  # 예: 피보나치
    
    return dp[n]
```

---

## 예제: 계단 오르기

```python
# [예제: 계단 오르기]
def climb_stairs(n):
    if n <= 2:
        return n
    
    dp = [0] * (n + 1)
    dp[1] = 1
    dp[2] = 2
    
    for i in range(3, n + 1):
        dp[i] = dp[i-1] + dp[i-2]
    
    return dp[n]
```

---

## 예제: 최대 부분 합 (Kadane's Algorithm)

```python
# [예제: 최대 부분 합 (Kadane's Algorithm)]
def max_subarray_sum(arr):
    # 1. 초기화
    max_current = max_global = arr[0]
    
    # 2. 메인 루프
    for i in range(1, len(arr)):
        # 3. 점화식: 현재까지 포함 vs 새로 시작
        max_current = max(arr[i], max_current + arr[i])
        max_global = max(max_global, max_current)
    
    return max_global
```

---

## 구조 요약

```text
초기화(DP 테이블 + 베이스 케이스) → 상향식 루프 → 점화식 적용
```
