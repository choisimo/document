# MongoDB Configuration
# =====================

# Authentication
MONGO_USERNAME=admin
MONGO_PASSWORD=changeme_password

# Note: This stack uses Replica Set with keyfile authentication
# Run ./scripts/init-keyfile.sh before first startup
