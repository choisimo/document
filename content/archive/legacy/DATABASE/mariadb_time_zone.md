# mariadb 시간 변경

## 적용 범위와 확인 조건

설정 파일 경로와 허용되는 시간대 표현은 MariaDB 이미지·배포판·버전에 따라 달라질 수 있다. 변경 전에 실제로 로드되는 설정 파일을 확인하고, 변경 뒤에는 서버 재시작과 새 세션 연결을 거쳐 전역 시간대와 세션 시간대가 기대값인지 각각 조회한다. 파일 편집만으로 적용 완료라고 판정하지 않는다.


    vim /etc/mysql/mariadb.cnf
    default-time-zone = '+9:00'